# lita-tokenizers

Providing the detailed APIs of tokenizers in the LiTA app.

It includes both the [Rust endpoint](./crates/lita-tokenizers) as a lib crate and the [CLI](./crates/lita-tokenizers-cli).
